export default class Fighter {
    posX: number;
    posY: number;
    orientation: number;

    constructor(posX: number, posY: number, orientation: number) {
        this.posX = posX;
        this.posY = posY;
        this.orientation = orientation;
    }
}
